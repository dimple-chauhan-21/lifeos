// Temporary file to validate lint/format/typecheck tooling for Phase 1.2.
// Removed in Phase 1.4 once the real Next.js application is bootstrapped.

export function add(a: number, b: number): number {
  return a + b;
}
